# 本次更新计划：完成 2.0 的验证与交付

规划日期：2026-09-22。状态：候选实现与本机验收完成，跨平台 CI 和发布验证进行中；真实联调待提供运行输入。

本计划按已确认的“收尾现有 2.0 功能”安排。
本轮目标是让用户可以安装、完成一次真实搜索、建立监控，并可靠处理新商品通知。

## 规划依据

本次检查得到的现状如下，后续实施时需重新核对 Git 与发布状态：

- 本地分支为 `main`，HEAD 为 `1c23e1b48a9b1771baba475d90ab4affadf2e817`，与本次查询的 GitHub `main` 一致。
- 最新公开 Release 为 `v1.0.0`；当前没有开放 PR 或 Issue。
- 新增本计划前，工作区有 23 个已跟踪文件被修改、26 个未跟踪文件。现有实现尚未进入提交，不能作为已发布功能。
- 本地 `VERSION` 为 `2.0.0-rc.1`，统一入口已有 13 个命令。`CHANGELOG.md` 将新功能列入 2.0 RC，但 `ROADMAP.md` 又将其中部分列在 2.1、2.2，版本范围不一致。
- 已实际运行 `version` 和 `demo`，均退出 0 且 `ok: true`。demo 使用合成数据，不代表真实闲鱼、AI 或通知服务联调成功。
- 当前工作流定义了 Linux、macOS、Windows × Python 3.10/3.12 六种 CI 组合，但远端最近的成功运行对应旧提交，不能覆盖未提交改动。
- tag 工作流已有正式包构建和校验，只执行 Actions artifact 上传，尚未配置 GitHub Release 附件发布。

以上是规划阶段的检查快照；实施进展见文末。

## 本次范围

将工作区已有的下列能力统一归入 2.0，逐项验证后交付，不再把同一实现重复列为未来版本：

| 用户环节 | 已有实现 | 本次需要完成的工作 |
|---|---|---|
| 初次使用 | `demo`、`doctor`、`state`、`setup` | 核对安装到首搜路径、失败后的下一步及文档示例 |
| 搜索与监控 | 搜索边界、任务更新/导入导出、schema 3、持久 outbox | 验证旧任务兼容、去重、基线、异常后恢复 |
| 可选分析与通知 | `analyze`、`evaluate`、`deliver` | 验证输入输出、外发结果与本地持久化之间的关系 |
| 安装与分发 | `version`、`install --check`、可复现 bundle | 验证独立安装，完成可下载的版本化发布产物 |

本次不扩展多关键词研究、详情抓取、价格历史、MCP 或 Web UI。候选版发现的真实缺陷优先修复。

## 实施顺序与验收

### 1. P0：整理版本范围与现有改动

从当前工作区建立集成分支并保留全部已有改动。按实际依赖组织提交，先区分产品代码、测试、文档和与发布无关的申请材料，避免整目录无差别提交。

统一 `VERSION`、`CHANGELOG.md`、`ROADMAP.md`、README 和命令发现信息。2.1、2.2 中已经包含在本次 RC 的能力移到 2.0；未实现或无法验证的条目保留为待办。

特别核对“本地可读摘要”：目前能看到通知文本生成和 AI 摘要，尚不能据此确认存在独立的离线结果摘要入口。澄清文档承诺即可，不为补齐措辞临时增加命令。

验收：每项 2.0 承诺均有明确入口、实现位置和验证方式；13 个公开命令及现有 schema 版本与实际代码一致。正式版版本号和发布日期只在完成验收后更新。

### 2. P0：验证监控、存储与投递的完整流程

复用现有测试，重点审查下列行为；仅对发现的缺陷或关键覆盖缺口补测试：

- 老版本任务可读取，迁移后任务定义与去重历史不丢失；超出新边界的旧任务有明确修复方式。
- 首次基线不通知存量；重复运行不重复入队；新商品与 seen ID 在同一次原子提交中持久化。
- 搜索后取消、清理失败或提交状态不明时，输出保留准确的持久化证据，已记录的待投递事件仍可恢复。
- 外部明确确认成功后才 ack；超时或 ack 不确定时保留事件并提示可能重复。验收按 at-least-once 语义，不承诺外部通知绝不重复。
- AI 分析失败不影响原始搜索结果和监控状态；预览摘要与最终请求匹配，模型输出不能改变商品身份。

验收：相关故障用例通过，随后在待合并提交上通过现有完整离线检查。失败、零结果、未投递和投递状态不明能够明确区分。

### 3. P1：完成首次使用和升级体验

README 首屏保留三条清楚的路径：离线体验、第一次真实搜索、持续监控。详细故障语义和高级参数链接到 references。

核对 `setup` 在缺依赖、缺状态、状态失效和搜索失败时的下一步；验证从其他工作目录以及 copy 安装后执行命令。保留可选 AI、通知模块的独立入口。

为 v1 用户补充升级说明：页数/尝试次数边界、缺失任务文件的错误变化、任务格式迁移、安装健康状态，以及升级前保存旧任务数据的方式。回退程序与恢复数据分别说明，避免直接用旧程序写入新格式。

验收：按文档可以走通安装与离线体验；旧用户能识别不兼容变化并保留原始数据。文档示例与 CLI 参数一致。

### 4. P1：完成候选验收与发布交付

依次完成以下步骤：

1. 在集成提交上运行 Ruff、格式检查、pytest 和 bundle self-check；PR CI 验证六种平台/Python 组合。
2. 验证两次构建字节一致，校验 manifest、SBOM、SHA-256，确认解包和空 HOME copy 安装后的 demo 一致。
3. 使用已授权的状态路径和明确关键词做一次一页、一次尝试的真实搜索。选定 AI 端点和通知适配器后，各完成一次预览摘要匹配的实际调用；其余适配器如只有离线验证，明确记录覆盖范围。
4. 联调只记录版本、退出码、计数、能力/持久化/外发状态；不将登录文件、端点密钥或真实商品文本写入公共记录。
5. 修复发现的问题，必要时产生下一 RC。通过后形成稳定版提交，完成合并，再在干净的精确版本 tag 上构建正式包。
6. 为 GitHub Release 附上经过校验的 bundle、SHA-256 和升级说明，实际检查附件可下载并与发布 commit 一致。Actions artifact 上传本身不算完成公开发布。

真实联调需要具体关键词、状态路径、AI 端点和通知目标；实施时沿用已有明确授权，只在缺少这些关键输入时补充。当前规划不发起联调或发布。

验收：稳定版 tag、版本文件、发布包来源和 Release 附件一致；离线与真实验证结果分别可核查，未验证的外部能力不标记为通过。

## 完成判据

本次更新完成意味着：现有改动已进入可追溯提交，版本范围一致，当前提交通过跨平台与打包验证，选定的真实使用路径完成联调，用户能下载、安装并按说明升级至该版本。

仅有本地代码、离线 demo 成功或旧提交 CI 绿灯，均不代表本次更新已经完成。

## 后续候选方向

2.0 交付后优先收集真实使用反馈，再安排选品质量提升。候选顺序为：可解释地排除配件/求购/租赁等结果，受控多关键词合并去重，最后再做价格与可售状态历史。届时再定义范围、数据依据和验收标准。

## 实施进展（2026-09-22）

- 已统一 2.0 范围，补齐首次使用、v1 升级和回退说明；未引入额外命令。
- 已修复发送超时/取消时遗漏重复风险提示、通知成功码类型校验，以及企业微信 UTF-8 字节限制和链接截断问题。
- 新增回归覆盖发送中取消、部分投递后超时、非整数确认码、中文/emoji 消息、过长链接，以及 schema 1/2 迁移时保留既有去重记录。
- 本机 Ruff、格式检查通过；pytest 为 655 passed、1 skipped（Windows 专用用例）。bundle 自检确认可复现、校验通过、空 HOME 安装成功、离线 demo 一致。
- Skill 校验、工作流 YAML/脚本语法、按版本提取发布说明及错误 tag 拒绝检查通过。
- 首轮跨平台 CI 发现 Windows 测试夹具自动使用 CRLF，提前触发严格校验文件检查；已改为精确 ASCII/LF 字节写入，并保留发布器的严格校验。
- 已补齐 tag 工作流的 Release 草稿、附件上传/下载校验和候选版本标记；跨平台及实际发布结果以后续 CI 为准。
- 尚未取得本次真实搜索关键词与授权状态路径，当前进程也未配置 AI/通知端点。保持 `2.0.0-rc.1`，不宣称真实联调通过或稳定版完成。
